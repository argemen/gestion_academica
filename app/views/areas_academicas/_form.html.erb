<%= form_for(@area_academica) do |f| %>
  <% if @area_academica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area_academica.errors.count, "error") %> prohibited this area_academica from being saved:</h2>

      <ul>
      <% @area_academica.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
